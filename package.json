{
	"name": "motorsports-calendar",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"generate-types": "cd .. && for f in *.schema.json; do out=\"../src/types/$(basename $f .schema.json).ts\"; npx json-schema-to-typescript \"$f\" -o \"$out\"; done"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/luxon": "^3.7.1",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.53.0"
	},
	"dependencies": {
		"cheerio": "^1.1.2",
		"luxon": "^3.7.2"
	}
}
